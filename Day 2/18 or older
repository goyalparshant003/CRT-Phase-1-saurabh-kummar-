#include <iostream>
using namespace std;

int main() {
    int age;

    cout << "Enter age of person A: ";
    cin >> age;

    if (age >= 18) {
        cout << "Person A is 18 or older." << endl;
    } else {
        cout << "Person A is younger than 18." << endl;
    }

    return 0;
}
